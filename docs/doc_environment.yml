name: dit_env

dependencies:
  - colorama
  - matplotlib
  - numpy >= 1.6
  - scipy >= 1.2.0
  - sphinx >= 1.3
  - pip:
    - boltons
    - debtcollector
    - hypothesis
    - ipython[nbconvert]
    - python-ternary
    - sarge
    - sphinxcontrib-bibtex
